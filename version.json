v1.2.2
- Added editable typical start time field for each location in Settings
- Custom times override built-in defaults and persist via autosave + JSON
- Shift hint when adding entries now reflects your custom per-location times
